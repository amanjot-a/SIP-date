# SENSEX SIP Timing Engine

Find statistically better SIP dates using historical SENSEX data.

- Analyzes drops, volatility, drawdowns, and panic days  
- Ranks best SIP weekdays, dates, weeks, and months  
- Explainable, rule-based, no ML


### # *CSV* files are the one's containing the outputs produced by model


## Run
```bash
pip install pandas numpy matplotlib seaborn scipy
python SensexAdvancedSip.py

# Data
Date, Open, High, Low, Close
